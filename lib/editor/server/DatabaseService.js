var SQL="SELECT n.nspname,p.confkey, 	    c.relname, 	    f.attnum AS number, 	    f.attname AS name, 	    f.attnum, 	    f.attnotnull AS notnull, 	    pg_catalog.format_type(f.atttypid,f.atttypmod) AS type, 	    CASE 		WHEN p.contype = 'p' THEN 't' 		ELSE 'f' 	    END AS primarykey, 	    CASE 		WHEN p.contype = 'u' THEN 't' 		ELSE 'f' 	    END AS uniquekey, 	    CASE 		WHEN p.contype = 'f' THEN g.relname 	    END AS foreignkey, 	    CASE 		WHEN p.contype = 'f' THEN p.confkey 	    END AS foreignkey_fieldnum, 	    CASE 		WHEN p.contype = 'f' THEN g.relname 	    END AS foreignkey, 	    CASE 		WHEN p.contype = 'f' THEN p.conkey 	    END AS foreignkey_connnum, 	    CASE 		WHEN f.atthasdef = 't' THEN d.adsrc 	    END AS default FROM pg_attribute f     JOIN pg_class c ON c.oid = f.attrelid     JOIN pg_type t ON t.oid = f.atttypid     LEFT JOIN pg_attrdef d ON d.adrelid = c.oid AND d.adnum = f.attnum     LEFT JOIN pg_namespace n ON n.oid = c.relnamespace     LEFT JOIN pg_constraint p ON p.conrelid = c.oid AND f.attnum = ANY (p.conkey)     LEFT JOIN pg_class AS g ON p.confrelid = g.oid WHERE c.relkind = 'r'::char                  AND f.attnum >= 0      ORDER BY n.nspname,c.relname,f.attnum",MODULE="Database Service | ",TOIGNORE={information_schema:!0,pg_catalog:!0},DB=require("uw").DB,LOG=require("uw").log,getTables=function(){return DB.query(SQL).then(function(a){for(var b=a.rows,c={},d="",e=0;e<b.length;e++){var f=b[e];if(f.relname!=d){if(TOIGNORE[f.nspname])continue;d=f.relname,c[d]={name:d,fields:{}}}c[d].fields[f.name]={type:f.type,name:f.name,pk:"t"==f.primarykey,fk:null!=f.foreignkey,fktable:f.foreignkey,fkfield:f.foreignkey_fieldnum,id:f.number}}return c})};module.exports.allservice=function(a,b){getTables().then(function(a){b.send(a)})["catch"](function(a){b.send(500,a)})};